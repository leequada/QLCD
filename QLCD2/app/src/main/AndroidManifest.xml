<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qlcd">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icob"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icob"
        android:supportsRtl="true"
        android:theme="@style/Theme.QLCD">
        <activity android:name=".View.Add_Activity"></activity>
        <activity android:name=".View.DetailCitizen_Activity" />
        <activity android:name=".View.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>